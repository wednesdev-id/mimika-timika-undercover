# Core dependencies for Vercel deployment
fastapi>=0.104.0
jinja2>=3.1.0
python-multipart>=0.0.6

# Data processing (minimal version for serverless)
requests>=2.28.0
beautifulsoup4>=4.11.0
lxml>=4.9.0

# Environment variables
python-dotenv>=0.19.0

# Note: For serverless deployment on Vercel:
# - uvicorn is not needed (handled by Vercel runtime)
# - pandas, openpyxl, schedule removed for smaller package size
# - For full functionality with scraping, consider Railway, Render, or PythonAnywhere

# Uncomment these if you need them in production:
# pandas>=1.5.0
# openpyxl>=3.0.10
# schedule>=1.2.0

# For dynamic sites with JavaScript (optional):
# selenium>=4.8.0
# webdriver-manager>=3.8.0